import React from 'react';
import Bar from '../shared/Bar';

import s from './Array.module.scss';

const Array = () => {
  return (
    <div className={s.array}>
      <Bar />
    </div>
  );
};

export default Array;
